model Item {
  id          Int      @id @default(autoincrement())
  name        String
  description String
  price       Int
  tags        String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  deletedAt   DateTime
}
